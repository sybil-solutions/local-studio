# Controller E2E Tests

This directory is reserved for controller tests that exercise a running controller process over HTTP. Integration tests that instantiate the Hono app in-process live in `tests/controller/integration/`.

// Moved to shared/agent/sanitize-embedded-browser-url.ts so the agent runtime
// package's browser host can share it; re-exported here for frontend callers.
export * from "@shared/agent/sanitize-embedded-browser-url";
